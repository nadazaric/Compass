<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- text box -->
    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
        <!--<Setter Property="Width" Value="250"/>-->
        <Setter Property="FontSize" Value="{StaticResource Dim.RegularFontSize}"/>
        <Setter Property="FontWeight" Value="Regular"/>
        <Setter Property="Height" Value="{StaticResource Dim.InputAndButtonHeight}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Background" Value="{StaticResource Color.PrimaryLightColor}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Color.PrimaryDarkColor}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Dim.BorderTickness}"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Margin" Value="0 0 0 10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{StaticResource Dim.RegularCorner}">
                        <ScrollViewer x:Name="PART_ContentHost"
                            Focusable="false"
                            HorizontalScrollBarVisibility="Hidden"
                            VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- button -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Color.PrimaryDarkColor}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Color.PrimaryDarkColor}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="{StaticResource Dim.RegularFontSize}"/>
        <Setter Property="Height" Value="{StaticResource Dim.InputAndButtonHeight}"/>
        <Setter Property="Margin" Value="0 10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{StaticResource Dim.BorderTickness}"
                        CornerRadius="{StaticResource Dim.RegularCorner}">
                        <ContentPresenter 
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource Color.ButtonHover}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Color.ButtonHover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
